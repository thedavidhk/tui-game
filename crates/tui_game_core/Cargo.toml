[package]
name = "tui_game_core"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
serde = { version = "1.0", features = ["derive"] }
ron = "0.8"
unicode-width = "0.2"

[dev-dependencies]
